'use strict';import { readFileSync } from 'node:fs';const data = readFileSync('./readFileSync.ts', 'utf8');const processedData = data.split('\n')    .filter((line) => !!line)    .join('');console.log('processedData: ', processedData);